---
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: external-app
  namespace: example-apps
  annotations:
    argocd.argoproj.io/sync-wave: "26"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  hosts:
    - httpbinn.org
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  location: MESH_EXTERNAL
  resolution: DNS
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: external-app
  namespace: example-apps
  annotations:
    argocd.argoproj.io/sync-wave: "26"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  host: httpbinn.org
  trafficPolicy:
    tls:
      mode: SIMPLE
      sni: httpbinn.org
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: external-app
  namespace: example-apps
  annotations:
    argocd.argoproj.io/sync-wave: "27"
spec:
  parentRefs:
    - name: main-gateway
      namespace: istio-ingress
      {{- if .Values.helloWorldApp.oidc.enabled }}
      sectionName: external-app
      {{- end }}
  hostnames:
    - external-app.{{ .Values.baseDns }}
  rules:
    - filters:
        - type: URLRewrite
          urlRewrite:
            hostname: httpbinn.org
      backendRefs:
        - group: networking.istio.io
          kind: Hostname
          name: httpbinn.org
          port: 443
